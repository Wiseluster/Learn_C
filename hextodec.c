#include <stdio.h>

int main(void)
{
	int hex;
	char ch;
	printf("Welcome to ASCll Code to Word Program.\n\n");

        while("true")
        {
		printf("Enter your number: ");
		scanf("%x", &hex);

		printf("Chang the hex(%x) to dec: %d \n", dec, dec);
	}
	return 0;
}
